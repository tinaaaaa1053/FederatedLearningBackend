# FastAPI + MySQL Backend

## Run Instructions

To run the backend, make sure you have the required dependencies installed and then use the following command:

```bash
uvicorn app.main:app --host 0.0.0.0 --port 3000
```

## Installation

To install dependencies, run:

```bash
pip install -r requirements.txt
```
